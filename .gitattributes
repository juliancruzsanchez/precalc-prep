# Track large binary media files via Git LFS so the repo stays clonable.
# Install LFS once: `brew install git-lfs && git lfs install`
App/Resources/Content/Videos/*.mp4 filter=lfs diff=lfs merge=lfs -text
